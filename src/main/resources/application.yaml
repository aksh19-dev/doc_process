spring:
  application:
    name: doc_process
  cloud:
    aws:
      credentials:
        access-key: test
        secret-key: test
      sqs:
        queue-name: doc-process-queue
        queue-url: https://localhost.localstack.cloud:4566/000000000000/doc-process-queue

aws:
  region:
    static: ap-south-1
  endpoint: http://localhost.localstack.cloud:4566
  s3:
    bucket-name: doc-process-bucket
  sns:
    topic-name: doc-process-topic
  dynamo-db:
    table-name: doc-process-table
  credentials:
    access-key: test
    secret-key: test
